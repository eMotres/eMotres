// Canonical public site URL — single source of truth for metadataBase, canonical
// tags, sitemap, robots and JSON-LD. The Google Ads campaign and the brand
// ("AeroStator Core") live on this domain; emotres.com 301-redirects here.
// Note: e-mail stays on @emotres.com (the corporate domain) — unrelated to this.
export const SITE_URL = 'https://aerostator.com';
